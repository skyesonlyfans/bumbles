import React from 'react';
import ReactDOM from 'react-dom/client';
import BumblesSimulator from './app';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BumblesSimulator />
  </React.StrictMode>,
);